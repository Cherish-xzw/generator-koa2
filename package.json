{
  "name": "generator-upoem-koa2",
  "version": "1.1.8",
  "description": "A simple koa2 boilerplate based on webpack3",
  "scripts": {
    "init": "git submodule init",
    "update": "git submodule update",
    "sync": "git submodule foreach git pull origin master",
    "test": "nyc mocha --timeout 9999999 && nyc report --reporter=text-lcov | coveralls"
  },
  "main": "app/index.js",
  "files": [
    "app"
  ],
  "author": "travisxu <zhengwei.xu@upchina.com>",
  "keywords": [
    "yeoman-generator"
  ],
  "dependencies": {
    "download-git-repo": "^1.0.1",
    "ora": "^1.3.0",
    "rimraf": "^2.6.2",
    "yeoman-generator": "^1.0.0"
  },
  "devDependencies": {
    "coveralls": "^3.0.0",
    "mocha": "^3.4.2",
    "nyc": "^11.3.0",
    "yeoman-assert": "^3.0.0",
    "yeoman-test": "^1.7.0"
  }
}
